#pragma once
#include "SmartPointer.h"

// TEZCAT_API void* operator new(std::size_t size);
// TEZCAT_API void operator delete(void* memory, std::size_t size);
// TEZCAT_API void* operator new[](std::size_t size);
// TEZCAT_API void operator delete[](void* memory, std::size_t size);
